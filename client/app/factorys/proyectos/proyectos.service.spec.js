/*
'use strict';

describe('Service: proyectos', function () {

  // load the service's module
  beforeEach(module('ss2AppApp'));

  // instantiate service
  var proyectos;
  beforeEach(inject(function (_proyectos_) {
    proyectos = _proyectos_;
  }));

  it('should do something', function () {
    expect(!!proyectos).toBe(true);
  });

});
*/